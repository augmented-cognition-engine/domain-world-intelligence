# World Intelligence ACE 0.7E activation conformance work packet (v1)

**Status:** bounded consumer candidate. Coordinated by Codex task
`019ff3c3-1b97-7712-aed9-998c0d808aa8`. This packet authorizes a World repository commit, branch
push, and draft PR only. It does not authorize an ACE Core edit, merge, tag, release, credential,
network capture, external action, or package publication.

## Outcome

Prove that the existing inert `world_intelligence_federal_register_monitor` Domain Pack uses the
unchanged ACE 0.7E compiler, conformance, v1alpha2 onboarding-plan admission, Core commit, and
consumer runtime contracts. The journey must produce World-specific identities, preview every
effect/capability/authority before approval, reload after restart, require new approval for upgrade
and rollback, bind current activation material into Monitor, persona/Subscription, Shift, and
canonical Brief resources, and fail before authority or persistence for stale conformance.

## Fixed dependency boundary

- Core source identity: `10bbed620291ac5f552c3313dd37580938a5b9d7`, stacked on
  `dab0866af239af9a13b4d2772a0d3950f932fa2e`.
- Accepted read-only candidate wheel:
  `/tmp/ace-07e-wheel-proof-019ff3fa/dist/ace_core-0.6.0-py3-none-any.whl`.
- Exact byte identity: 5,955,226 bytes and SHA-256
  `19b75ab8dd2e2cc69f432a97fd7401eb0f55c9b5b7e2deeed0ae17e2396dff57`.
- The `/tmp` wheel is ephemeral and has no durable uploaded byte-retrieval coordinate. The Core
  commit is source identity only and is not a substitute for wheel provenance.
- Core is consumed as an installed distribution. No Core checkout enters `PYTHONPATH`, and no Core
  file is edited.

## Owned consumer material

- one golden categorical-transition fixture beside the existing Federal Register monitor pack;
- one provider-free acceptance harness using public installed Core APIs only;
- positive and negative consumer tests; and
- this work packet plus the evidence record.

The pack remains JSON-only and World-owned. It declares a Federal Register agency monitor,
official-publication transition, general-reader/public-researcher routes, an official-record Brief,
one `source_snapshot` capability, and one bounded `source_read` authority request. It declares no
credentials, connector transport, delivery, provider, persistence, or external-action effect.

## Acceptance sequence

1. Independently verify the exact Core wheel filename, size, and SHA-256, then install it in a clean
   Python 3.12 target.
2. Compile the installed World pack and run the new golden fixture through
   `run_domain_pack_conformance`.
3. Run the unchanged 0.7D Connect → Map → World Watch edit/approve → first Brief flow and close it
   into a non-authorizing `activation-onboarding-handoff/v1alpha2`.
4. Preview the closed five-effect set plus the exact capability and authority material.
5. Approve and admit the exact initial plan, reload it from a fresh service, then admit an overlay
   upgrade under a new plan/approval.
6. Roll back to the exact earlier active revision under another new plan/approval and reload again.
7. Validate the historical commit reference and prove it remains `live_authority=false`; reject it
   when presented as a runtime activation revision.
8. Bind the current rollback revision into World Monitor, persona/Subscription, Shift, and canonical
   Brief contracts.
9. Present drifted conformance to an empty store and assert zero approvals, zero grants, and zero
   committed heads.
10. Build the World wheel, install both artifacts into clean targets, and reproduce the same exact
    World identities without a Core source checkout.

## Non-claims and rollback

This proves provider-free contract composition over recorded/public fixture material. It does not
claim network freshness, legal effect, autonomous publication, deployed scheduling, or that a
historical reference grants authority. Code rollback removes the additive harness, fixture, tests,
and documentation; already demonstrated content-addressed receipts remain evidence, not deployed
state.
