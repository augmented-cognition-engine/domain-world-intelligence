# ACE World Intelligence 0.7.0-rc1 — public proof surface

**Release status:** developer-preview release candidate  
**Scenario:** `meridia_reservoir_release_72h`  
**Mode:** PREPARED / FROZEN  
**Data:** synthetic, hermetic, and redistributable — not LIVE

## What is now concrete

One command runs the accepted World Intelligence scenario through the public ACE Core +
Intelligence contracts and produces both a machine-readable proof and a self-contained visual
Reality Brief:

```bash
PYTHONPATH=/path/to/domain-world-intelligence:/path/to/ace-core \
  /path/to/ace-core/.venv/bin/python scripts/public_demo.py
```

Outputs:

- `artifacts/public-demo/demo-data.json`
- `artifacts/public-demo/index.html`

The published extension wheel remains an inert Domain Pack consumer of ACE Core + Intelligence.
The visual command intentionally stays in the source repository for this developer-preview release;
it does not inject demo harnesses into the installed runtime.

The page does not use a second demo engine or a hand-built substitute result. It runs the accepted
derivation-family and supersession-impact harnesses, then refuses to render if their release-critical
identities or counts drift.

## The proof

Two publishers do not necessarily provide two independent sources. Coastal Wire and Harborview are
different publishers, but both derive from the Ledger report and therefore collapse into the same
derivation family. The Basin gauge is a distinct root. Only the Ledger + Basin pairing satisfies the
Brief's two-family corroboration rule.

When the later Ledger correction is admitted, ACE appends a governed impact projection:

| Boundary | Accepted result |
|---|---:|
| Downstream resources affected | 11 |
| Brief claims affected | 9 |
| Direct dependencies | 6 |
| Transitive dependencies | 5 |
| Unaffected resources | 16 |
| Maximum traversal depth | 3 |

Impact means dependency, not falsehood. The projection reports what depended on the superseded
record. It does not issue a truth verdict. The original Brief is not rewritten and replays
byte-identically under its original cutoff without another reasoning call.

Exact public identities:

- Case: `case:412426eee708d56f6bda931ccf9e5d8b`
- Brief: `brief:25d8232c9bfa27050bdcb160fb75f06c`
- Derivation-family projection:
  `brief_derivation_family_status_projection:3500889a2d75af7a5484a681afbee34c`
- Supersession-impact projection:
  `supersession_impact_projection:f3723de8e9ac5c4390c5c46137f3765e`

## Release guardrails

- This is a frozen synthetic scenario, not a live news product.
- Provenance structure and epistemic status are not claims that ACE has determined objective truth.
- Independence is only as strong as the lineage admitted to the graph.
- The numbers above describe this scenario; they are not capacity, accuracy, or scale benchmarks.
- This release candidate does not claim production readiness, autonomous publishing, or external action.

The next product boundary is a governed, read-only LIVE official-source adapter. It must not weaken
the prepared replay or the trust labels demonstrated here.
